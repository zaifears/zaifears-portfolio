export const metadata = {
  title: 'University Result Analyzer',
  description: 'Analyze university examination results with interactive charts and detailed reports',
};

export default function ResultAnalyzerLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
